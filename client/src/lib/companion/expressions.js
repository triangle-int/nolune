// @ts-check
/** Stub: implemented after the tests. */
export const MOON_BODY = "";
export const EXPRESSIONS = Object.freeze([]);
/** @param {string} kind @param {{ reducedMotion?: boolean }} [options] */
export function companionExpression(kind, options = {}) {
	throw new Error(`companionExpression not implemented (${kind}, ${JSON.stringify(options)})`);
}
/** @param {string} expression */
export function expressionAsset(expression) {
	throw new Error(`expressionAsset not implemented (${expression})`);
}
/** @param {string} expression @param {string} fill */
export function eyesMarkup(expression, fill) {
	throw new Error(`eyesMarkup not implemented (${expression}, ${fill})`);
}
