[package]
name = "cua-protocol"
version = "0.1.0"
edition = "2024"
description = "Shared Nolune Cua machine protocol and capability policy"
license = "MIT"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
